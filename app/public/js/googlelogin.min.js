(function() {
  const e = function(a) {
    return (
      !!document.currentScript &&
      (document.currentScript.src.indexOf(`?${a}`) != -1 ||
        document.currentScript.src.indexOf(`&${a}`) != -1)
    );
  };
  const f = e('loadGamesSDK') ? '/cast_game_sender.js' : '/cast_sender.js';
  const g = e('loadCastFramework') || e('loadCastApplicationFramework');
  const h = function() {
    return typeof window.__onGCastApiAvailable === 'function'
      ? window.__onGCastApiAvailable
      : null;
  };
  const k = [
    'pkedcjkdefgpdelpbcmbmeomcjbeemfm',
    'enhhojjnijigcajfphajepfemndkmdlo',
  ];
  var m = function(a) {
    a.length
      ? l(a.shift(), function() {
        m(a);
        })
      : n();
  };
  const p = function(a) {
    return `chrome-extension://${a}${f}`;
  };
  var l = function(a, c, b) {
    const d = document.createElement('script');
    d.onerror = c;
    b && (d.onload = b);
    d.src = a;
    (document.head || document.documentElement).appendChild(d);
  };
  var n = function() {
    const a = h();
    a && a(!1, 'No cast extension found');
  };
  const q = function() {
    if (g) {
      let a = 2;
      const c = h();
      const b = function() {
        a--;
        a == 0 && c && c(!0);
      };
      window.__onGCastApiAvailable = b;
      l('//www.gstatic.com/cast/sdk/libs/sender/1.0/cast_framework.js', n, b);
    }
  };
  if (
    window.navigator.userAgent.indexOf('Android') >= 0 &&
    window.navigator.userAgent.indexOf('Chrome/') >= 0 &&
    window.navigator.presentation
  ) {
    q();
    const r = window.navigator.userAgent.match(/Chrome\/([0-9]+)/);
    m([
      `//www.gstatic.com/eureka/clank/${r ? parseInt(r[1], 10) : 0}${f}`,
      `//www.gstatic.com/eureka/clank${f}`,
    ]);
  } else
    !window.chrome ||
    !window.navigator.presentation ||
    window.navigator.userAgent.indexOf('Edge') >= 0
      ? n()
      : (q(), m(k.map(p)));
})();
