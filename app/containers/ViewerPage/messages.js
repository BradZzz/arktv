/*
 * ViewerPage Messages
 *
 * This contains all the text for the ViewerPage component.
 */
import { defineMessages } from 'react-intl';

export const scope = 'boilerplate.containers.ViewerPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'ArkTv2',
  },
});
